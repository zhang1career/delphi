"use strict";

/**
 * Same-origin path handled by Metro in development (see metro.config.js).
 * The browser does not send config secrets; Metro forwards them server-side.
 */
exports.WEB_DEV_CONFIG_PROXY_PATH = "/__expo_dev_config_proxy__";
